#!/usr/bin/env bash

export SCRIPTPATH="$( cd -- "$(dirname "`readlink -f $0`")" >/dev/null 2>&1 ; pwd -P )"

export PYTHONPATH="$SCRIPTPATH"/../dist/wasm/

python-wasm-debug "$PYTHONPATH"/cython.py "$@"