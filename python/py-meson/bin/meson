#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "$(readlink -f "$0")")" >/dev/null 2>&1 && pwd -P)"
export PYTHONPATH="${script_dir}/../dist/wasm${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m mesonbuild.mesonmain "$@"
