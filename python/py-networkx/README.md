# NetworkX Python Library For WebAssembly Python

This packages the pure Python `networkx` wheel for CoWasm.

The WASI smoke covers Sage-relevant graph operations including connectivity,
DAG ordering, graph isomorphism, weighted shortest paths, flows/cuts, greedy
coloring, independent sets, and Cartesian graph products.
