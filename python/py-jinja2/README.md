# Jinja2

This package provides a small CoWasm bundle of Jinja2 and MarkupSafe's pure
Python fallback. It is used as Sagelite build support for Meson/Cython
generators that import `jinja2` under the Node.js/Electron milestone.
