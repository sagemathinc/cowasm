# Mutation Class Database For SageMath

This package provides SageMath's `database_mutation_class` data package for
CoWasm. Sage uses these finite mutation class tables for cluster algebra and
quiver computations.

CoWasm installs the `.dig6` pickle data under
`dist/wasm/share/mutation_class`.
