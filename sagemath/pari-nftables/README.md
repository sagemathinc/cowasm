# PARI Nftables For SageMath

This package provides SageMath's optional `pari_nftables` data package for
CoWasm. PARI uses `nftables` for tabulated number fields of degrees 2 through
7.

CoWasm installs the files under `dist/wasm/share/pari/nftables`, matching
PARI's shared-data layout.
