# Matroid Database For SageMath

This package provides SageMath's optional `matroid_database` data package for
CoWasm. Sage uses it for enumerating matroids and unorientable matroids from
the Matsumoto database.

CoWasm installs the Python package and compressed data files under
`dist/wasm/matroid_database`.
