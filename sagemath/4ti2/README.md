# 4ti2

4ti2 is a SageMath dependency for algebraic, geometric, and combinatorial
computations on linear spaces.

This package builds a static WASI SDK standalone baseline against the existing
CoWasm GMP and GLPK ports. The smoke verifies arbitrary-precision Groebner
basis computation through `4ti2gmp` and Hilbert-basis computation through
`zsolve`.
