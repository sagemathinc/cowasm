# Symbolic Data For SageMath

This package provides SageMath's `database_symbolic_data` package for CoWasm.
It contains XML benchmark systems for integer and general polynomial systems,
including cyclic, Katsura, geometry, and curve examples.

CoWasm installs the XML resources under
`dist/wasm/share/symbolic_data/XMLResources`.
