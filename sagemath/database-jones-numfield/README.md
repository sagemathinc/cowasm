# Jones Number-Field Database For SageMath

This package provides SageMath's `database_jones_numfield` data package for
CoWasm. Sage uses John Jones's tables when looking up number fields with
prescribed degree, discriminant, and ramification data.

CoWasm installs the Sage object archive under
`dist/wasm/share/jones_numfield`.
