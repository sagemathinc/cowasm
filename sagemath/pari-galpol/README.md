# PARI Galpol For SageMath

This package provides SageMath's optional `pari_galpol` data package for
CoWasm. PARI uses `galpol` for polynomials defining Galois extensions of the
rationals.

CoWasm installs the files under `dist/wasm/share/pari/galpol`, matching PARI's
shared-data layout.
