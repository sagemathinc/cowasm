# Qhull compiled to WebAssembly

This package provides the reentrant static Qhull library for WebAssembly
builds. It installs the library as `libqhull_r.a`, matching the library name
expected by matplotlib when `system_qhull` is enabled.
