--- native/src/options.c	2022-10-01 09:10:53.000000000 -0700
+++ wasm/src/options.c	2022-10-16 15:01:39.000000000 -0700
@@ -169,6 +169,9 @@
 		shellparam.nparam++;
 		xargv++;
 	}
+	/* CoWasm: do not force libedit on by default.  With the current
+	 * runtime/libedit combination, el_gets can report EOF immediately
+	 * in a real terminal, which makes an interactive shell exit at once. */
 	optschanged();
 
 	return login;
