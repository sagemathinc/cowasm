#!/usr/bin/env bash

export SCRIPTPATH="$( cd -- "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )"
export PACKAGES="$SCRIPTPATH"/../packages/
export TERMCAP="$PACKAGES"/cpython/src/termcap
export TERM=xterm-256color

node "$PACKAGES"/wasi/bin/run.js "$PACKAGES"/cpython/dist/wasm/bin/python3.wasm $@